/**
 * hostUrl is base Url path
 */
const hostUrl = "https://http-hunt.thoughtworks-labs.net";

/**
 * challengeUrl is a path or route for the sample API
 */
const challengeUrl = "/challenge";

/**
 * getInputUrl is a path or route for the get input API
 */
const getInputUrl = "/input";

/**
 * getOutputUrl is a path or route for the get output API
 */
const getOutputUrl = "/output";

/**
 * exporting this declared configs to use anywhere in the project
 */
export { hostUrl, challengeUrl, getInputUrl, getOutputUrl };
